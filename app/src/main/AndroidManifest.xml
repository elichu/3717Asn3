<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.bcit.ass3.chu_stannus_liu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddItemToEventActivity"/>
        <activity android:name=".DisplayItemListActivity" />
        <activity android:name=".DisplayEventDetailActivity" />
        <activity android:name=".DisplayEventListActivity"/>
        <activity android:name=".EditEventActivity" />
        <activity android:name=".DeleteEventActivity" />
        <activity android:name=".CreateEventActivity" />
        <activity android:name=".EditItemActivity" />
        <activity android:name=".SearchResultsActivity" />

    </application>

</manifest>